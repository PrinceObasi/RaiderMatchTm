name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  check:
    name: Lint & Type Check
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.2

      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Audit dependencies
        run: bun run audit

      - name: Type check
        run: |
          bunx tsc -p tsconfig.app.json --noEmit
          bunx tsc -p tsconfig.node.json --noEmit

      - name: Lint
        run: bunx eslint .

      - name: Test
        run: bun run test

      - name: Check Edge Functions
        run: |
          deno test supabase/functions/_shared
          deno test supabase/functions/validate-internship-links/validation_test.ts
          deno check supabase/functions/validate-internship-links/index.ts
          deno check supabase/functions/apply/index.ts
          deno check supabase/functions/delete-account/index.ts
          deno check supabase/functions/notify_employer/index.ts
          deno check supabase/functions/update-company-logos/index.ts
          deno check supabase/functions/match/index.ts
          deno check supabase/functions/auto-sync-simplify/index.ts
          deno check supabase/functions/enrich-internship/index.ts
          deno check supabase/functions/enrich-missing/index.ts
          deno check supabase/functions/send-welcome/index.ts
          deno check supabase/functions/weekly-digest/index.ts
          deno test --config supabase/functions/upload-resume/deno.json supabase/functions/upload-resume/resume-parser_test.ts
          deno test --config supabase/functions/upload-resume/deno.json supabase/functions/upload-resume/resume-upload-validation_test.ts
          deno check --config supabase/functions/upload-resume/deno.json supabase/functions/upload-resume/index.ts

      - name: Build
        run: bun run build
        env:
          VITE_SUPABASE_URL: https://placeholder.supabase.co
          VITE_SUPABASE_PUBLISHABLE_KEY: placeholder
